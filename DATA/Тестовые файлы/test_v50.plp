--============================================================
-- Тестовый файл для правила: v50
-- Описание: outer join,oracle,ansi
-- Категория: п.1.1
-- Дата генерации: 2026-05-04 07:12:47
--============================================================

--------------------------------------------------------------
-- = НЕПРАВИЛЬНЫЙ КОД (ДО ИСПРАВЛЕНИЯ) =
--------------------------------------------------------------
В Oracle используется оператор `(+)` для внешнего соединения. В PostgreSQL и ANSI SQL его нет. Заменить на явные `LEFT JOIN` или `RIGHT JOIN` с условием `ON`.

--------------------------------------------------------------
-- = ПРАВИЛЬНЫЙ КОД (ПОСЛЕ ИСПРАВЛЕНИЯ) =
--------------------------------------------------------------
`select * from t1, t2 where t1.id = t2.id(+)`

--============================================================
-- Конец файла
--============================================================