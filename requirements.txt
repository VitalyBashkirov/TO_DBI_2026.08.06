# === АРМ "Адаптация под DBI" — зависимости ===
# Стандартная библиотека Python 3.10 (входит в дистрибутив)
# tkinter, os, json, re, subprocess, shutil, pathlib, datetime
# threading, zipfile, typing, logging, dataclasses, argparse, io, sys, codecs

# === Внешние зависимости (установить через pip) ===
# tkinter — встроен в python:3.10-slim на Linux (пакет python3-tk)
# На Windows/Mac tkinter встроен в базовый образ python

# === Опциональные зависимости для headless-режима ===
# Если потребуется парсинг YAML для конфигураций:
# pyyaml>=6.0

# === Опциональные зависимости для логирования ===
# colorama>=0.4.6  # цветной вывод в консоль (опционально)

# === Зависимости для линтинга (dev-only, не в prod-образ) ===
# flake8>=6.0.0
# black>=23.0.0
# isort>=5.12.0

# === Зависимости для тестирования (dev-only) ===
# pytest>=7.0.0

# === Примечание ===
# Все модули проекта используют только стандартную библиотеку Python 3.10.
# Внешних зависимостей pip нет.
# tkinter предоставляется через базовый образ python:3.10-slim (apt-пакет python3-tk).
