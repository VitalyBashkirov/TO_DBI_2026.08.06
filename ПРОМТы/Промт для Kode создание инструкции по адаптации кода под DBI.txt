# Промт для Kode: создание инструкции по адаптации кода под DBI

## Роль
Ты — эксперт по адаптации PLPlus кода под архитектуру DBI (PostgreSQL/Oracle) на основе документа «Рекомендации по адаптации кода на PLPlus для работы с DBI приложением v50.docx».

## Задача
Составь инструкцию для разработчиков в формате Markdown (.md) со следующими требованиями.

## Требования к инструкции

### 1. Заголовок
`# Инструкция по адаптации кода под DBI для Kode`

### 2. Оглавление
Со ссылками на все разделы.

### 3. Обязательные разделы

#### 3.1. Что нужно знать перед началом
- Основные принципы DBI (таблица: принцип → значение)
- Цветовая маркировка из документа (синий, фиолетовый, оранжевый, серый)

#### 3.2. SQL/DML — самые частые изменения
- Outer join: `(+)` → `LEFT/RIGHT JOIN` (пример ❌ Было / ✅ Стало)
- Ограничение выборки: `rownum` → `FETCH FIRST` (пример)
- `DECODE` → `CASE` (пример)
- Иерархические запросы: `CONNECT BY` → `WITH RECURSIVE` (пример)
- UDF в SQL: где запрещено, где можно (пример)
- Системные представления Oracle: запрещённые, разрешённые, альтернативы (таблица)

#### 3.3. Хранение данных
- Тип `DATE` → `DATE_TIME` для PostgreSQL (особенности, пример)
- Временные таблицы: избавляться, альтернативы `VARRAY`, пример `init_temp_table`
- Уникальные индексы с NULL (различие Oracle/PostgreSQL)
- Размер BTREE индекса (ограничение 2704 байта, пути решения)

#### 3.4. Процедурный код
- Замена Oracle пакетов (таблица: Oracle → DBI-альтернатива)
- `Execute immediate`: правила (экранирование, сброс кэша)
- `WHEN OTHERS`: обязательный rollback или raise (пример ❌ и ✅)
- Bulk операции: поддержка, ограничения

#### 3.5. Интеграции
- Замена Database Link на REST/SOAP (таблица: задача → приложение → версия)
- Очереди: Oracle AQ или Apache ActiveMQ, управление через `[RUNTIME].[AQ_LIB]`

#### 3.6. Кэширование
- Уровни кэша Hibernate (таблица: уровень, где, размер, жизнь)
- Разыменование ссылки = обращение к кэшу (пример)
- Совет: получать поля сразу в курсоре (пример ✅)
- Настройка кэша 2 уровня (пример XML)

#### 3.7. Чек-лист разработчика
Разделы с галочками `- [ ]`:
- SQL запросы
- Хранение
- Процедурный код
- Интеграции
- Кэш

#### 3.8. Где брать актуальную информацию
Таблица: источник → что там

#### 3.9. Памятка для быстрого поиска проблем
Таблица: «Если ищете...» → «Искать в коде» → «Что делать»

### 4. Требования к оформлению
- Подсветка синтаксиса для SQL и PL+: использовать ````sql`
- Для неправильного кода: `❌ Было (Oracle)`
- Для правильного: `✅ Стало (ANSI SQL / DBI)`
- Таблицы в Markdown где уместно
- Стиль: деловой, технический, без воды

### 5. Дополнительные условия
- Не упоминать PLPCheck
- Не упоминать Git-репозиторий
- Не использовать сложные корпоративные внутренние ссылки

## Результат
Сгенерируй полный текст .md файла, готовый к сохранению.