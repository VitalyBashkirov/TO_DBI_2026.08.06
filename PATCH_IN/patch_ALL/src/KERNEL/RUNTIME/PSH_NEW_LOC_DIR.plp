class RUNTIME;

@import_plsql(true)
@this(create_on_start:=true)
@name('[ПСБ] Вызов создания директории на клиенте из клиент-скрипта')
@tag('COPIED')
static method PSH_NEW_LOC_DIR is

@name('Путь к загружаемым файлам')              public V_PATH     [FILE$PATH];
@name('Подтверждать создание директории (Y/N)') public V_ASK_USER [STRING_1];
end;
