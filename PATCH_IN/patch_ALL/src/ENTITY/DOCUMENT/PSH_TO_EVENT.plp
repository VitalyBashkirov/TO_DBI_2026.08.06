class DOCUMENT;

@import_plsql(true)
@name('[ПСБ] Добавить в обработку событий')
method PSH_TO_EVENT is
PSH_TO_EVENT (
	@name('Дополнительная информация') P_ADD_INFO in [MEMO] default null
) is

execute is
begin
	/*if this%class = 'MAIN_DOCUM' then
		for c in this.[HISTORY_STATE]
		where c.[STATE] like 'GMP_REQ%'
		loop
			c.[DELETE#AUTO];
		end loop;
	end if;*/
	
	::[PSH_EVENT].[PSH_NEW#AUTO](
		this%class,	/*P_OBJ_CLASS*/		--Класс
		this%id		/*P_OBJ_ID*/		--Экземпляр
		,P_ADD_INFO == P_ADD_INFO 
	);
end;
end;
end;
