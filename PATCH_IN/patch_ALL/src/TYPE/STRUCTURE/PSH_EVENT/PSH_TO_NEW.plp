class PSH_EVENT;

@import_plsql(true)
@name('[ПСБ] На повторную обработку')
method PSH_TO_NEW is
PSH_TO_NEW is

execute is
begin
	[PSH_LIB].to_new(this); -- updated CHANGE_SHORT_NAME LIB->PSH_LIB 18/12/25
end;
end;
end;
