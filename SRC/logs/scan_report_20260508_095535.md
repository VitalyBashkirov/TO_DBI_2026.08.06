# Отчёт анализа PLPlus кода

Дата: 2026-05-08 09:55:35
Всего найдено проблем: 119
Всего файлов: 1

## Итоговая статистика по типам проблем

- v50.SQL.UDF.п.1.3: 12
- v50.PROC.EXECUTE.п.3.3: 7
- v50.SQL.ORACLE_PKG.п.1.11: 7
- v50.SQL.ROWNUM.п.1.2: 7
- v50.PROC.NATIVEID.п.3.26: 6
- v50.SQL.CAST.п.1.5: 6
- v50.PROC.DYNAMIC_SQL.п.3.3: 5
- v50.PROC.ID_SIZE.п.3.27: 4
- v50.SQL.BIND_GROUP.п.1.26: 4
- v50.SQL.CONTEXT.п.1.23: 4
- v50.SQL.SYSTABLES.п.1.10: 4
- v50.SQL.SYSVIEW.п.1.9: 4
- v50.SQL.VW_CRIT_RPT.п.1.19: 4
- v50.PROC.EMPTY_STRING.п.3.2: 3
- v50.PROC.TYPING.п.3.11: 3
- v50.PROC.UTILS.п.3.1.1: 3
- v50.PROC.WHENOTHERS.п.3.5: 3
- v50.SQL.OUTERJOIN.п.1.1: 3
- v50.STOR.INDEX.п.2.6.3: 3
- v50.INT.QUEUE.п.4.4: 2
- v50.PROC.BULK.п.1.28: 2
- v50.PROC.CRYPTO.п.3.1.2: 2
- v50.PROC.JSON.п.3.1.4: 2
- v50.PROC.SMTP.п.3.1.3: 2
- v50.PROC.XML.п.3.6: 2
- v50.SQL.CONNECTBY.п.1.8: 2
- v50.SQL.DECODE.п.1.6.1: 2
- v50.SQL.DML_JOIN.п.1.17: 2
- v50.INT.DBLINK.п.4.2: 1
- v50.PROC.CHARSET.п.3.16: 1
- v50.PROC.CODEGEN.п.3.15: 1
- v50.PROC.EXCEPTIONLOOP.п.1.25: 1
- v50.PROC.HTTP.п.3.1.6: 1
- v50.PROC.TRIGGER.п.3.8: 1
- v50.STOR.DATE.п.2.2: 1
- v50.STOR.TEMP.п.2.8: 1
- v50.STOR.UNIQUE_IDX.п.2.6.2: 1

---

## Файл: F:\TO_DBI\PATCH_IN\patch_TEST_DS\TestDeepSeek_full.plp

**Всего проблем в файле:** 119

**Проблемы по типам**:

- v50.SQL.UDF.п.1.3: 12
- v50.PROC.EXECUTE.п.3.3: 7
- v50.SQL.ORACLE_PKG.п.1.11: 7
- v50.SQL.ROWNUM.п.1.2: 7
- v50.PROC.NATIVEID.п.3.26: 6
- v50.SQL.CAST.п.1.5: 6
- v50.PROC.DYNAMIC_SQL.п.3.3: 5
- v50.PROC.ID_SIZE.п.3.27: 4
- v50.SQL.BIND_GROUP.п.1.26: 4
- v50.SQL.CONTEXT.п.1.23: 4
- v50.SQL.SYSTABLES.п.1.10: 4
- v50.SQL.SYSVIEW.п.1.9: 4
- v50.SQL.VW_CRIT_RPT.п.1.19: 4
- v50.PROC.EMPTY_STRING.п.3.2: 3
- v50.PROC.TYPING.п.3.11: 3
- v50.PROC.UTILS.п.3.1.1: 3
- v50.PROC.WHENOTHERS.п.3.5: 3
- v50.SQL.OUTERJOIN.п.1.1: 3
- v50.STOR.INDEX.п.2.6.3: 3
- v50.INT.QUEUE.п.4.4: 2
- v50.PROC.BULK.п.1.28: 2
- v50.PROC.CRYPTO.п.3.1.2: 2
- v50.PROC.JSON.п.3.1.4: 2
- v50.PROC.SMTP.п.3.1.3: 2
- v50.PROC.XML.п.3.6: 2
- v50.SQL.CONNECTBY.п.1.8: 2
- v50.SQL.DECODE.п.1.6.1: 2
- v50.SQL.DML_JOIN.п.1.17: 2
- v50.INT.DBLINK.п.4.2: 1
- v50.PROC.CHARSET.п.3.16: 1
- v50.PROC.CODEGEN.п.3.15: 1
- v50.PROC.EXCEPTIONLOOP.п.1.25: 1
- v50.PROC.HTTP.п.3.1.6: 1
- v50.PROC.TRIGGER.п.3.8: 1
- v50.STOR.DATE.п.2.2: 1
- v50.STOR.TEMP.п.2.8: 1
- v50.STOR.UNIQUE_IDX.п.2.6.2: 1

---

### Строка 89

**Проблема:** v50.PROC.TYPING.п.3.11 - REF объявление

```plp
	v_ref REF [PRICE_LIST];
```

**Было  :** `	v_ref REF [PRICE_LIST];`
**Станет:** `	v_ref REF [PRICE_LIST];`

---

### Строка 97

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Левая часть без (+), правая с (+)

```plp
	select * from t1, t2 where t1.id = t2.id(+);
```

**Было  :** `	select * from t1, t2 where t1.id = t2.id(+);`
**Станет:** `	select * from t1, t2 where t1 left join t2 on t1.id = t2.id;`

---

### Строка 97

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
	select * from t1, t2 where t1.id = t2.id(+);
```

**Было  :** `	select * from t1, t2 where t1.id = t2.id(+);`
**Станет:** `	select * from t1, t2 where t1.id = t2.id(+);`

---

### Строка 126

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			ac%id = gj.[ACCOUNT](true)
```

**Было  :** `			ac%id = gj.[ACCOUNT](true)`
**Станет:** `			ac%id = gj.[ACCOUNT](true)`

---

### Строка 127

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку (без квадратных скобок) с параметром true

```plp
			and gj.[IN_FILE_HISTORY] = st.collection_id(true)
```

**Было  :** `			and gj.[IN_FILE_HISTORY] = st.collection_id(true)`
**Станет:** `			and gj.[IN_FILE_HISTORY] = st.collection_id(true)`

---

### Строка 209

**Проблема:** v50.SQL.ROWNUM.п.1.2 - ROWNUM в условии WHERE

```plp
		select * from t where rownum = 1;
```

**Было  :** `		select * from t where rownum = 1;`
**Станет:** `		select * from t where fetch first 1 rows only;`

---

### Строка 209

**Проблема:** v50.SQL.ROWNUM.п.1.2 - Любое использование ROWNUM

```plp
		select * from t where rownum = 1;
```

**Было  :** `		select * from t where rownum = 1;`
**Станет:** `		select * from t where fetch first 1 rows only;`

---

### Строка 212

**Проблема:** v50.SQL.ROWNUM.п.1.2 - Любое использование ROWNUM

```plp
		select * from t where rownum <= 10;
```

**Было  :** `		select * from t where rownum <= 10;`
**Станет:** `		select * from t where fetch first 10 rows only;`

---

### Строка 215

**Проблема:** v50.SQL.ROWNUM.п.1.2 - Любое использование ROWNUM

```plp
		select * from t where rownum between 5 and 10;
```

**Было  :** `		select * from t where rownum between 5 and 10;`
**Станет:** `		select * from t where rownum between 5 and 10;`

---

### Строка 218

**Проблема:** v50.SQL.ROWNUM.п.1.2 - ROWNUM в условии WHERE

```plp
		select id, name from clients where rownum < 100 order by name;
```

**Было  :** `		select id, name from clients where rownum < 100 order by name;`
**Станет:** `		select id, name from clients where rownum < 100 order by name;`

---

### Строка 218

**Проблема:** v50.SQL.ROWNUM.п.1.2 - Любое использование ROWNUM

```plp
		select id, name from clients where rownum < 100 order by name;
```

**Было  :** `		select id, name from clients where rownum < 100 order by name;`
**Станет:** `		select id, name from clients where rownum < 100 order by name;`

---

### Строка 279

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		select * from t where my_func(id) > 0;
```

**Было  :** `		select * from t where my_func(id) > 0;`
**Станет:** `		select * from t where my_func(id) > 0;`

---

### Строка 282

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в GROUP BY

```plp
		select my_func(id) from t group by my_func(id);
```

**Было  :** `		select my_func(id) from t group by my_func(id);`
**Станет:** `		select my_func(id) from t group by my_func(id);`

---

### Строка 285

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в ORDER BY

```plp
		select * from t order by my_func(name);
```

**Было  :** `		select * from t order by my_func(name);`
**Станет:** `		select * from t order by my_func(name);`

---

### Строка 288

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		select * from documents where calc_amount(doc_id) > 1000;
```

**Было  :** `		select * from documents where calc_amount(doc_id) > 1000;`
**Станет:** `		select * from documents where calc_amount(doc_id) > 1000;`

---

### Строка 342

**Проблема:** v50.SQL.CAST.п.1.5 - Сложение строки с числом

```plp
		select c_code + 1 from "Z#DEPART";
```

**Было  :** `		select c_code + 1 from "Z#DEPART";`
**Станет:** `		select c_code + 1 from "Z#DEPART";`

---

### Строка 345

**Проблема:** v50.SQL.CAST.п.1.5 - LIKE оператор (проверить тип поля)

```plp
		select * from t where numeric_column like '123%';
```

**Было  :** `		select * from t where numeric_column like '123%';`
**Станет:** `		select * from t where numeric_column like '123%';`

---

### Строка 357

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		select * from t where to_char(numeric_column) like '123%';
```

**Было  :** `		select * from t where to_char(numeric_column) like '123%';`
**Станет:** `		select * from t where to_char(numeric_column) like '123%';`

---

### Строка 357

**Проблема:** v50.SQL.CAST.п.1.5 - LIKE оператор (проверить тип поля)

```plp
		select * from t where to_char(numeric_column) like '123%';
```

**Было  :** `		select * from t where to_char(numeric_column) like '123%';`
**Станет:** `		select * from t where to_char(numeric_column) like '123%';`

---

### Строка 396

**Проблема:** v50.SQL.DECODE.п.1.6.1 - Вызов DECODE

```plp
		select decode(status,1,'A',2,'B','?') from t;
```

**Было  :** `		select decode(status,1,'A',2,'B','?') from t;`
**Станет:** `		select decode(status,1,'A',2,'B','?') from t;`

---

### Строка 399

**Проблема:** v50.SQL.DECODE.п.1.6.1 - Вызов DECODE

```plp
		select decode(flag, 1, 'One', 2, 'Two', 3, 'Three', 'Unknown') from config;
```

**Было  :** `		select decode(flag, 1, 'One', 2, 'Two', 3, 'Three', 'Unknown') from config;`
**Станет:** `		select decode(flag, 1, 'One', 2, 'Two', 3, 'Three', 'Unknown') from config;`

---

### Строка 442

**Проблема:** v50.SQL.CONNECTBY.п.1.8 - CONNECT BY

```plp
		select * from t connect by prior id = parent_id start with parent_id is null;
```

**Было  :** `		select * from t connect by prior id = parent_id start with parent_id is null;`
**Станет:** `		select * from t connect by prior id = parent_id start with parent_id is null;`

---

### Строка 445

**Проблема:** v50.SQL.CONNECTBY.п.1.8 - CONNECT BY

```plp
		select level, id, name from employees connect by prior id = manager_id start with manager_id is null;
```

**Было  :** `		select level, id, name from employees connect by prior id = manager_id start with manager_id is null;`
**Станет:** `		select level, id, name from employees connect by prior id = manager_id start with manager_id is null;`

---

### Строка 461

**Проблема:** v50.SQL.CAST.п.1.5 - Сложение строки с числом

```plp
			select e2.level + 1, e.id, e.name from employees e join emp_cte e2 on e.manager_id = e2.id
```

**Было  :** `			select e2.level + 1, e.id, e.name from employees e join emp_cte e2 on e.manager_id = e2.id`
**Станет:** `			select e2.level + 1, e.id, e.name from employees e join emp_cte e2 on e.manager_id = e2.id`

---

### Строка 498

**Проблема:** v50.SQL.SYSVIEW.п.1.9 - V$ представления

```plp
		select * from v$session;
```

**Было  :** `		select * from v$session;`
**Станет:** `		select * from v$session;`

---

### Строка 501

**Проблема:** v50.SQL.SYSVIEW.п.1.9 - GV$ представления

```plp
		select sid, serial# from gv$session where username = 'APP_USER';
```

**Было  :** `		select sid, serial# from gv$session where username = 'APP_USER';`
**Станет:** `		select sid, serial# from gv$session where username = 'APP_USER';`

---

### Строка 504

**Проблема:** v50.SQL.CAST.п.1.5 - LIKE оператор (проверить тип поля)

```plp
		select * from user_tables where table_name like 'Z#%';
```

**Было  :** `		select * from user_tables where table_name like 'Z#%';`
**Станет:** `		select * from user_tables where table_name like 'Z#%';`

---

### Строка 504

**Проблема:** v50.SQL.SYSVIEW.п.1.9 - USER_ представления

```plp
		select * from user_tables where table_name like 'Z#%';
```

**Было  :** `		select * from user_tables where table_name like 'Z#%';`
**Станет:** `		select * from user_tables where table_name like 'Z#%';`

---

### Строка 507

**Проблема:** v50.SQL.SYSVIEW.п.1.9 - ALL_ представления

```plp
		select * from all_objects where owner = 'IBS';
```

**Было  :** `		select * from all_objects where owner = 'IBS';`
**Станет:** `		select * from all_objects where owner = 'IBS';`

---

### Строка 550

**Проблема:** v50.SQL.SYSTABLES.п.1.10 - Обращение к системным таблицам ТЯ

```plp
		select * from classes;
```

**Было  :** `		select * from classes;`
**Станет:** `		select * from classes;`

---

### Строка 553

**Проблема:** v50.SQL.SYSTABLES.п.1.10 - Обращение к системным таблицам ТЯ

```plp
		select id, name from methods where class_id = 100;
```

**Было  :** `		select id, name from methods where class_id = 100;`
**Станет:** `		select id, name from methods where class_id = 100;`

---

### Строка 556

**Проблема:** v50.SQL.CAST.п.1.5 - LIKE оператор (проверить тип поля)

```plp
		select * from criteria where name like '%FILTER%';
```

**Было  :** `		select * from criteria where name like '%FILTER%';`
**Станет:** `		select * from criteria where name like '%FILTER%';`

---

### Строка 556

**Проблема:** v50.SQL.SYSTABLES.п.1.10 - Обращение к системным таблицам ТЯ

```plp
		select * from criteria where name like '%FILTER%';
```

**Было  :** `		select * from criteria where name like '%FILTER%';`
**Станет:** `		select * from criteria where name like '%FILTER%';`

---

### Строка 559

**Проблема:** v50.SQL.SYSTABLES.п.1.10 - Обращение к системным таблицам ТЯ

```plp
		select * from rtl_entries where entry_name = 'TEST';
```

**Было  :** `		select * from rtl_entries where entry_name = 'TEST';`
**Станет:** `		select * from rtl_entries where entry_name = 'TEST';`

---

### Строка 601

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
	v_file UTL_FILE.FILE_TYPE;
```

**Было  :** `	v_file UTL_FILE.FILE_TYPE;`
**Станет:** `	v_file UTL_FILE.FILE_TYPE;`

---

### Строка 606

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
		utl_file.put_line(v_file, 'text');
```

**Было  :** `		utl_file.put_line(v_file, 'text');`
**Станет:** `		utl_file.put_line(v_file, 'text');`

---

### Строка 609

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
		dbms_output.put_line('message');
```

**Было  :** `		dbms_output.put_line('message');`
**Станет:** `		dbms_output.put_line('message');`

---

### Строка 612

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
		dbms_alert.signal('event', 'data');
```

**Было  :** `		dbms_alert.signal('event', 'data');`
**Станет:** `		dbms_alert.signal('event', 'data');`

---

### Строка 615

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
		dbms_pipe.send_message('pipe', 'data');
```

**Было  :** `		dbms_pipe.send_message('pipe', 'data');`
**Станет:** `		dbms_pipe.send_message('pipe', 'data');`

---

### Строка 618

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
		dbms_random.value();
```

**Было  :** `		dbms_random.value();`
**Станет:** `		dbms_random.value();`

---

### Строка 621

**Проблема:** v50.SQL.ORACLE_PKG.п.1.11 - Oracle пакеты

```plp
		utl_raw.cast_to_raw('text');
```

**Было  :** `		utl_raw.cast_to_raw('text');`
**Станет:** `		utl_raw.cast_to_raw('text');`

---

### Строка 659

**Проблема:** v50.SQL.DML_JOIN.п.1.17 - UPDATE/DELETE с JOIN

```plp
		delete from (select * from t1 join t2 on t1.id = t2.id);
```

**Было  :** `		delete from (select * from t1 join t2 on t1.id = t2.id);`
**Станет:** `		delete from (select * from t1 join t2 on t1.id = t2.id);`

---

### Строка 662

**Проблема:** v50.SQL.DML_JOIN.п.1.17 - UPDATE/DELETE с JOIN

```plp
		update (select t1.status, t2.code from t1 join t2 on t1.id = t2.id) set status = 'CLOSED';
```

**Было  :** `		update (select t1.status, t2.code from t1 join t2 on t1.id = t2.id) set status = 'CLOSED';`
**Станет:** `		update (select t1.status, t2.code from t1 join t2 on t1.id = t2.id) set status = 'CLOSED';`

---

### Строка 668

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		delete from t1 where exists (select 1 from t2 where t2.id = t1.id);
```

**Было  :** `		delete from t1 where exists (select 1 from t2 where t2.id = t1.id);`
**Станет:** `		delete from t1 where exists (select 1 from t2 where t2.id = t1.id);`

---

### Строка 671

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		update t1 set status = 'CLOSED' where exists (select 1 from t2 where t2.id = t1.id);
```

**Было  :** `		update t1 set status = 'CLOSED' where exists (select 1 from t2 where t2.id = t1.id);`
**Станет:** `		update t1 set status = 'CLOSED' where exists (select 1 from t2 where t2.id = t1.id);`

---

### Строка 705

**Проблема:** v50.SQL.VW_CRIT_RPT.п.1.19 - SELECT из VW_CRIT/VW_RPT/VW_SQL

```plp
		select * from VW_SQL where id = 100;
```

**Было  :** `		select * from VW_SQL where id = 100;`
**Станет:** `		select * from VW_SQL where id = 100;`

---

### Строка 708

**Проблема:** v50.SQL.VW_CRIT_RPT.п.1.19 - SELECT из VW_CRIT/VW_RPT/VW_SQL

```plp
		select * from VW_CRIT where status = 'ACTIVE';
```

**Было  :** `		select * from VW_CRIT where status = 'ACTIVE';`
**Станет:** `		select * from VW_CRIT where status = 'ACTIVE';`

---

### Строка 711

**Проблема:** v50.SQL.VW_CRIT_RPT.п.1.19 - SELECT из VW_CRIT/VW_RPT/VW_SQL

```plp
		select name, code from VW_RPT where date_begin > sysdate;
```

**Было  :** `		select name, code from VW_RPT where date_begin > sysdate;`
**Станет:** `		select name, code from VW_RPT where date_begin > sysdate;`

---

### Строка 717

**Проблема:** v50.SQL.VW_CRIT_RPT.п.1.19 - SELECT из VW_CRIT/VW_RPT/VW_SQL

```plp
		select * from VW_RPT where date_begin > sysdate;
```

**Было  :** `		select * from VW_RPT where date_begin > sysdate;`
**Станет:** `		select * from VW_RPT where date_begin > sysdate;`

---

### Строка 761

**Проблема:** v50.SQL.CONTEXT.п.1.23 - SYS_CONTEXT с USERENV

```plp
		v_dbname := sys_context('USERENV','DB_NAME');
```

**Было  :** `		v_dbname := sys_context('USERENV','DB_NAME');`
**Станет:** `		v_dbname := sys_context('USERENV','DB_NAME');`

---

### Строка 764

**Проблема:** v50.SQL.CONTEXT.п.1.23 - SYS_CONTEXT с USERENV

```plp
		v_sessionid := sys_context('USERENV','SESSIONID');
```

**Было  :** `		v_sessionid := sys_context('USERENV','SESSIONID');`
**Станет:** `		v_sessionid := sys_context('USERENV','SESSIONID');`

---

### Строка 767

**Проблема:** v50.SQL.CONTEXT.п.1.23 - SYS_CONTEXT с USERENV

```plp
		v_current_user := sys_context('USERENV','CURRENT_USER');
```

**Было  :** `		v_current_user := sys_context('USERENV','CURRENT_USER');`
**Станет:** `		v_current_user := sys_context('USERENV','CURRENT_USER');`

---

### Строка 770

**Проблема:** v50.SQL.CONTEXT.п.1.23 - SYS_CONTEXT с <OWNER>_SYSTEM

```plp
		v_current_user := sys_context('IBS_SYSTEM', 'USER');
```

**Было  :** `		v_current_user := sys_context('IBS_SYSTEM', 'USER');`
**Станет:** `		v_current_user := sys_context('IBS_SYSTEM', 'USER');`

---

### Строка 818

**Проблема:** v50.SQL.BIND_GROUP.п.1.26 - Bind переменная в GROUP BY

```plp
		select count(*) from t group by :bind_var;
```

**Было  :** `		select count(*) from t group by :bind_var;`
**Станет:** `		select count(*) from t group by :bind_var;`

---

### Строка 821

**Проблема:** v50.SQL.BIND_GROUP.п.1.26 - Bind переменная в ORDER BY

```plp
		select distinct status from orders order by :order_col;
```

**Было  :** `		select distinct status from orders order by :order_col;`
**Станет:** `		select distinct status from orders order by :order_col;`

---

### Строка 821

**Проблема:** v50.SQL.BIND_GROUP.п.1.26 - Bind переменная в DISTINCT

```plp
		select distinct status from orders order by :order_col;
```

**Было  :** `		select distinct status from orders order by :order_col;`
**Станет:** `		select distinct status from orders order by :order_col;`

---

### Строка 824

**Проблема:** v50.SQL.BIND_GROUP.п.1.26 - Bind переменная в GROUP BY

```plp
		select count(*) from t group by case when status = :v_status then 1 else 0 end;
```

**Было  :** `		select count(*) from t group by case when status = :v_status then 1 else 0 end;`
**Станет:** `		select count(*) from t group by case when status = :v_status then 1 else 0 end;`

---

### Строка 865

**Проблема:** v50.PROC.TYPING.п.3.11 - REF объявление

```plp
	v_cl_priv_ref REF [CL_PRIV];
```

**Было  :** `	v_cl_priv_ref REF [CL_PRIV];`
**Станет:** `	v_cl_priv_ref REF [CL_PRIV];`

---

### Строка 882

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		for rec in (select * from ::[CL_PRIV] where id in (select ...)) loop
```

**Было  :** `		for rec in (select * from ::[CL_PRIV] where id in (select ...)) loop`
**Станет:** `		for rec in (select * from ::[CL_PRIV] where id in (select ...)) loop`

---

### Строка 920

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	v_bad_date DATE;
```

**Было  :** `	v_bad_date DATE;`
**Станет:** `	v_bad_date DATE;`

---

### Строка 966

**Проблема:** v50.STOR.TEMP.п.2.8 - GLOBAL TEMPORARY TABLE

```plp
		create global temporary table t (id number) on commit preserve rows;
```

**Было  :** `		create global temporary table t (id number) on commit preserve rows;`
**Станет:** `		create global temporary table t (id number) on commit preserve rows;`

---

### Строка 1004

**Проблема:** v50.STOR.UNIQUE_IDX.п.2.6.2 - Уникальный индекс

```plp
		CREATE UNIQUE INDEX idx ON t (c1, c2);
```

**Было  :** `		CREATE UNIQUE INDEX idx ON t (c1, c2);`
**Станет:** `		CREATE UNIQUE INDEX idx ON t (c1, c2);`

---

### Строка 1011

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		DELETE FROM t WHERE rowid NOT IN (SELECT MIN(rowid) FROM t GROUP BY c1, c2);
```

**Было  :** `		DELETE FROM t WHERE rowid NOT IN (SELECT MIN(rowid) FROM t GROUP BY c1, c2);`
**Станет:** `		DELETE FROM t WHERE rowid NOT IN (SELECT MIN(rowid) FROM t GROUP BY c1, c2);`

---

### Строка 1045

**Проблема:** v50.STOR.INDEX.п.2.6.3 - CREATE INDEX

```plp
		create index idx on t(long_text);
```

**Было  :** `		create index idx on t(long_text);`
**Станет:** `		create index idx on t(long_text);`

---

### Строка 1050

**Проблема:** v50.STOR.INDEX.п.2.6.3 - CREATE INDEX

```plp
		create index idx on t using gin(long_text gin_trgm_ops);
```

**Было  :** `		create index idx on t using gin(long_text gin_trgm_ops);`
**Станет:** `		create index idx on t using gin(long_text gin_trgm_ops);`

---

### Строка 1087

**Проблема:** v50.PROC.CRYPTO.п.3.1.2 - DBMS_CRYPTO вызовы

```plp
		v_hash := dbms_crypto.hash(v_src, dbms_crypto.HASH_MD5);
```

**Было  :** `		v_hash := dbms_crypto.hash(v_src, dbms_crypto.HASH_MD5);`
**Станет:** `		v_hash := dbms_crypto.hash(v_src, dbms_crypto.HASH_MD5);`

---

### Строка 1087

**Проблема:** v50.PROC.CRYPTO.п.3.1.2 - DBMS_CRYPTO вызовы

```plp
		v_hash := dbms_crypto.hash(v_src, dbms_crypto.HASH_MD5);
```

**Было  :** `		v_hash := dbms_crypto.hash(v_src, dbms_crypto.HASH_MD5);`
**Станет:** `		v_hash := dbms_crypto.hash(v_src, dbms_crypto.HASH_MD5);`

---

### Строка 1124

**Проблема:** v50.PROC.SMTP.п.3.1.3 - UTL_SMTP вызовы

```plp
	v_connection UTL_SMTP.CONNECTION;
```

**Было  :** `	v_connection UTL_SMTP.CONNECTION;`
**Станет:** `	v_connection UTL_SMTP.CONNECTION;`

---

### Строка 1128

**Проблема:** v50.PROC.SMTP.п.3.1.3 - UTL_SMTP вызовы

```plp
		v_connection := utl_smtp.open_connection('smtp.server.com');
```

**Было  :** `		v_connection := utl_smtp.open_connection('smtp.server.com');`
**Станет:** `		v_connection := utl_smtp.open_connection('smtp.server.com');`

---

### Строка 1170

**Проблема:** v50.PROC.HTTP.п.3.1.6 - UTL_HTTP вызовы

```plp
		v_result := utl_http.request('http://example.com');
```

**Было  :** `		v_result := utl_http.request('http://example.com');`
**Станет:** `		v_result := utl_http.request('http://example.com');`

---

### Строка 1208

**Проблема:** v50.PROC.UTILS.п.3.1.1 - utils.session_id

```plp
		v_session_id := utils.session_id;
```

**Было  :** `		v_session_id := utils.session_id;`
**Станет:** `		v_session_id := utils.session_id;`

---

### Строка 1209

**Проблема:** v50.PROC.UTILS.п.3.1.1 - method_mgr.clear_object_refcing

```plp
		method_mgr.clear_object_refcing();
```

**Было  :** `		method_mgr.clear_object_refcing();`
**Станет:** `		method_mgr.clear_object_refcing();`

---

### Строка 1210

**Проблема:** v50.PROC.UTILS.п.3.1.1 - stdio.zip

```plp
		stdio.zip('file.txt');
```

**Было  :** `		stdio.zip('file.txt');`
**Станет:** `		stdio.zip('file.txt');`

---

### Строка 1251

**Проблема:** v50.PROC.EMPTY_STRING.п.3.2 - Сравнение с пустой строкой

```plp
		select count(*) into v_count from clients where name = '';
```

**Было  :** `		select count(*) into v_count from clients where name = '';`
**Станет:** `		select count(*) into v_count from clients where name is null;`

---

### Строка 1252

**Проблема:** v50.PROC.EMPTY_STRING.п.3.2 - Сравнение с пустой строкой

```plp
		v_name := '';
```

**Было  :** `		v_name := '';`
**Станет:** `		v_name :is null;`

---

### Строка 1252

**Проблема:** v50.PROC.EMPTY_STRING.п.3.2 - Присваивание пустой строки

```plp
		v_name := '';
```

**Было  :** `		v_name := '';`
**Станет:** `		v_name :is null;`

---

### Строка 1292

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'truncate table Z#TEMP';
```

**Было  :** `		execute immediate 'truncate table Z#TEMP';`
**Станет:** `		execute immediate 'truncate table "Z#TEMP"';`

---

### Строка 1293

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'select * from Z#CLIENT';
```

**Было  :** `		execute immediate 'select * from Z#CLIENT';`
**Станет:** `		execute immediate 'select * from "Z#CLIENT"';`

---

### Строка 1298

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'truncate table "Z#TEMP"';
```

**Было  :** `		execute immediate 'truncate table "Z#TEMP"';`
**Станет:** `		execute immediate 'truncate table ""Z#TEMP""';`

---

### Строка 1299

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'select * from "Z#CLIENT"';
```

**Было  :** `		execute immediate 'select * from "Z#CLIENT"';`
**Станет:** `		execute immediate 'select * from ""Z#CLIENT""';`

---

### Строка 1386

**Проблема:** v50.PROC.EXCEPTIONLOOP.п.1.25 - EXCEPTIONLOOP

```plp
		for i in 1..10 exceptionloop
```

**Было  :** `		for i in 1..10 exceptionloop`
**Станет:** `		for i in 1..10 exceptionloop`

---

### Строка 1436

**Проблема:** v50.PROC.BULK.п.1.28 - FORALL

```plp
		forall i in 1..100
```

**Было  :** `		forall i in 1..100`
**Станет:** `		forall i in 1..100`

---

### Строка 1443

**Проблема:** v50.PROC.BULK.п.1.28 - FORALL

```plp
		forall i in 1..100
```

**Было  :** `		forall i in 1..100`
**Станет:** `		forall i in 1..100`

---

### Строка 1475

**Проблема:** v50.PROC.JSON.п.3.1.4 - Oracle JSON типы

```plp
	v_json_obj JSON_Object_T;
```

**Было  :** `	v_json_obj JSON_Object_T;`
**Станет:** `	v_json_obj JSON_Object_T;`

---

### Строка 1480

**Проблема:** v50.PROC.JSON.п.3.1.4 - Oracle JSON типы

```plp
		v_json_obj := JSON_Object_T();
```

**Было  :** `		v_json_obj := JSON_Object_T();`
**Станет:** `		v_json_obj := JSON_Object_T();`

---

### Строка 1519

**Проблема:** v50.PROC.TRIGGER.п.3.8 - CREATE TRIGGER

```plp
		create trigger trg_update_client on clients after update as
```

**Было  :** `		create trigger trg_update_client on clients after update as`
**Станет:** `		create trigger trg_update_client on clients after update as`

---

### Строка 1563

**Проблема:** v50.PROC.XML.п.3.6 - DBMS_XMLDOM

```plp
	v_xml_doc DBMS_XMLDOM.DOMDocument;
```

**Было  :** `	v_xml_doc DBMS_XMLDOM.DOMDocument;`
**Станет:** `	v_xml_doc DBMS_XMLDOM.DOMDocument;`

---

### Строка 1568

**Проблема:** v50.PROC.XML.п.3.6 - DBMS_XMLDOM

```plp
		v_xml_doc := DBMS_XMLDOM.newDomDocument();
```

**Было  :** `		v_xml_doc := DBMS_XMLDOM.newDomDocument();`
**Станет:** `		v_xml_doc := DBMS_XMLDOM.newDomDocument();`

---

### Строка 1642

**Проблема:** v50.PROC.TYPING.п.3.11 - %rowtype объявление

```plp
	v_client_row CLIENT%rowtype;
```

**Было  :** `	v_client_row CLIENT%rowtype;`
**Станет:** `	v_client_row CLIENT%rowtype;`

---

### Строка 1684

**Проблема:** v50.PROC.CODEGEN.п.3.15 - DDL операций/представлений

```plp
		create operation MY_NEW_OPERATION is
```

**Было  :** `		create operation MY_NEW_OPERATION is`
**Станет:** `		create operation MY_NEW_OPERATION is`

---

### Строка 1726

**Проблема:** v50.PROC.CHARSET.п.3.16 - Запрещённые функции

```plp
		v_upper_name := nls_upper(v_name);
```

**Было  :** `		v_upper_name := nls_upper(v_name);`
**Станет:** `		v_upper_name := nls_upper(v_name);`

---

### Строка 1888

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link в SQL запросах

```plp
		select * from remote_table@remote_db;
```

**Было  :** `		select * from remote_table@remote_db;`
**Станет:** `		select * from remote_table@remote_db;`

---

### Строка 1924

**Проблема:** v50.INT.QUEUE.п.4.4 - DBMS_AQ

```plp
	v_enqueue_options DBMS_AQ.ENQUEUE_OPTIONS_T;
```

**Было  :** `	v_enqueue_options DBMS_AQ.ENQUEUE_OPTIONS_T;`
**Станет:** `	v_enqueue_options DBMS_AQ.ENQUEUE_OPTIONS_T;`

---

### Строка 1928

**Проблема:** v50.INT.QUEUE.п.4.4 - DBMS_AQ

```plp
		dbms_aq.enqueue(queue_name => 'test_queue', enqueue_options => v_enqueue_options);
```

**Было  :** `		dbms_aq.enqueue(queue_name => 'test_queue', enqueue_options => v_enqueue_options);`
**Станет:** `		dbms_aq.enqueue(queue_name => 'test_queue', enqueue_options => v_enqueue_options);`

---

### Строка 2121

**Проблема:** v50.SQL.ROWNUM.п.1.2 - Любое использование ROWNUM

```plp
		select rownum, name from clients;
```

**Было  :** `		select rownum, name from clients;`
**Станет:** `		select rownum, name from clients;`

---

### Строка 2165

**Проблема:** v50.STOR.INDEX.п.2.6.3 - CREATE INDEX

```plp
		create index IDX_TEMP on Z#TEMP(id);
```

**Было  :** `		create index IDX_TEMP on Z#TEMP(id);`
**Станет:** `		create index IDX_TEMP on Z#TEMP(id);`

---

### Строка 2171

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'create table "' || v_schema || '"."Z#TEMP" (id number)';
```

**Было  :** `		execute immediate 'create table "' || v_schema || '"."Z#TEMP" (id number)';`
**Станет:** `		execute immediate 'create table "' || v_schema || '".""Z#TEMP"" (id number)';`

---

### Строка 2172

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'truncate table "' || v_schema || '"."Z#TEMP"';
```

**Было  :** `		execute immediate 'truncate table "' || v_schema || '"."Z#TEMP"';`
**Станет:** `		execute immediate 'truncate table "' || v_schema || '".""Z#TEMP""';`

---

### Строка 2173

**Проблема:** v50.PROC.EXECUTE.п.3.3 - EXECUTE IMMEDIATE

```plp
		execute immediate 'create index "' || v_schema || '"."IDX_TEMP" on "' || v_schema || '"."Z#TEMP"(id)';
```

**Было  :** `		execute immediate 'create index "' || v_schema || '"."IDX_TEMP" on "' || v_schema || '"."Z#TEMP"(id)';`
**Станет:** `		execute immediate 'create index "' || v_schema || '"."IDX_TEMP" on "' || v_schema || '".""Z#TEMP""(id)';`

---

### Строка 2207

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		where exists (select 1 from t fetch first 1 rows only);
```

**Было  :** `		where exists (select 1 from t fetch first 1 rows only);`
**Станет:** `		where exists (select 1 from t fetch first 1 rows only);`

---

### Строка 2213

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		where exists (select 1 from t);
```

**Было  :** `		where exists (select 1 from t);`
**Станет:** `		where exists (select 1 from t);`

---

### Строка 2250

**Проблема:** v50.PROC.DYNAMIC_SQL.п.3.3 - DBMS_SQL вызовы

```plp
		v_cursor := dbms_sql.open_cursor();
```

**Было  :** `		v_cursor := dbms_sql.open_cursor();`
**Станет:** `		v_cursor := dbms_sql.open_cursor();`

---

### Строка 2251

**Проблема:** v50.PROC.DYNAMIC_SQL.п.3.3 - DBMS_SQL вызовы

```plp
		dbms_sql.parse(v_cursor, 'select * from t', dbms_sql.native);
```

**Было  :** `		dbms_sql.parse(v_cursor, 'select * from t', dbms_sql.native);`
**Станет:** `		dbms_sql.parse(v_cursor, 'select * from t', dbms_sql.native);`

---

### Строка 2251

**Проблема:** v50.PROC.DYNAMIC_SQL.п.3.3 - DBMS_SQL вызовы

```plp
		dbms_sql.parse(v_cursor, 'select * from t', dbms_sql.native);
```

**Было  :** `		dbms_sql.parse(v_cursor, 'select * from t', dbms_sql.native);`
**Станет:** `		dbms_sql.parse(v_cursor, 'select * from t', dbms_sql.native);`

---

### Строка 2252

**Проблема:** v50.PROC.DYNAMIC_SQL.п.3.3 - DBMS_SQL вызовы

```plp
		v_rows := dbms_sql.execute(v_cursor);
```

**Было  :** `		v_rows := dbms_sql.execute(v_cursor);`
**Станет:** `		v_rows := dbms_sql.execute(v_cursor);`

---

### Строка 2253

**Проблема:** v50.PROC.DYNAMIC_SQL.п.3.3 - DBMS_SQL вызовы

```plp
		dbms_sql.close_cursor(v_cursor);
```

**Было  :** `		dbms_sql.close_cursor(v_cursor);`
**Станет:** `		dbms_sql.close_cursor(v_cursor);`

---

### Строка 1322

**Проблема:** v50.PROC.WHENOTHERS.п.3.5 - WHEN OTHERS без ROLLBACK/RAISE

```plp
--   examples.rollback.good: exception when others then rollback; raise;
```

**Было  :** `--   examples.rollback.good: exception when others then rollback; raise;`
**Станет:** `--   examples.rollback.good: exception when others then rollback; raise;`

---

### Строка 1337

**Проблема:** v50.PROC.WHENOTHERS.п.3.5 - WHEN OTHERS без ROLLBACK/RAISE

```plp
when others then
```

**Было  :** `when others then`
**Станет:** `when others then`

---

### Строка 1397

**Проблема:** v50.PROC.WHENOTHERS.п.3.5 - WHEN OTHERS без ROLLBACK/RAISE

```plp
when others then
```

**Было  :** `when others then`
**Станет:** `when others then`

---

### Строка 1780

**Проблема:** v50.PROC.NATIVEID.п.3.26 - NativeID для метаданных (NUMBER переменная для %id)

```plp
--   Пример кода для корректировки: v_id number; v_id := rMeth%id;
```

**Было  :** `--   Пример кода для корректировки: v_id number; v_id := rMeth%id;`
**Станет:** `--   Пример кода для корректировки: v_id number; v_id := rMeth%id;`

---

### Строка 1781

**Проблема:** v50.PROC.NATIVEID.п.3.26 - NativeID для метаданных (NUMBER переменная для %id)

```plp
--   Пример исправленного кода: v_id varchar2(100); v_id := rMeth%id;
```

**Было  :** `--   Пример исправленного кода: v_id varchar2(100); v_id := rMeth%id;`
**Станет:** `--   Пример исправленного кода: v_id varchar2(100); v_id := rMeth%id;`

---

### Строка 1788

**Проблема:** v50.PROC.NATIVEID.п.3.26 - NativeID для метаданных (NUMBER переменная для %id)

```plp
--   code_example_bad: v_id number; v_id := rMeth%id;
```

**Было  :** `--   code_example_bad: v_id number; v_id := rMeth%id;`
**Станет:** `--   code_example_bad: v_id number; v_id := rMeth%id;`

---

### Строка 1829

**Проблема:** v50.PROC.NATIVEID.п.3.26 - NativeID для метаданных (NUMBER переменная для %id)

```plp
--   code_example_bad: v_id varchar2(10); v_id := r%id;
```

**Было  :** `--   code_example_bad: v_id varchar2(10); v_id := r%id;`
**Станет:** `--   code_example_bad: v_id varchar2(10); v_id := r%id;`

---

### Строка 1805

**Проблема:** v50.PROC.NATIVEID.п.3.26 - NativeID для метаданных (NUMBER переменная для %id)

```plp
v_bad_id := rMeth%id;
```

**Было  :** `v_bad_id := rMeth%id;`
**Станет:** `v_bad_id := rMeth%id;`

---

### Строка 1846

**Проблема:** v50.PROC.NATIVEID.п.3.26 - NativeID для метаданных (NUMBER переменная для %id)

```plp
v_bad_id := client%id;
```

**Было  :** `v_bad_id := client%id;`
**Станет:** `v_bad_id := client%id;`

---

### Строка 1821

**Проблема:** v50.PROC.ID_SIZE.п.3.27 - Размер идентификатора бизнес-данных (менее 20 символов)

```plp
--   Пример кода для корректировки: v_id varchar2(10);
```

**Было  :** `--   Пример кода для корректировки: v_id varchar2(10);`
**Станет:** `--   Пример кода для корректировки: v_id varchar2(10);`

---

### Строка 1829

**Проблема:** v50.PROC.ID_SIZE.п.3.27 - Размер идентификатора бизнес-данных (менее 20 символов)

```plp
--   code_example_bad: v_id varchar2(10); v_id := r%id;
```

**Было  :** `--   code_example_bad: v_id varchar2(10); v_id := r%id;`
**Станет:** `--   code_example_bad: v_id varchar2(10); v_id := r%id;`

---

### Строка 1841

**Проблема:** v50.PROC.ID_SIZE.п.3.27 - Размер идентификатора бизнес-данных (менее 20 символов)

```plp
v_bad_id VARCHAR2(10);
```

**Было  :** `v_bad_id VARCHAR2(10);`
**Станет:** `v_bad_id VARCHAR2(10);`

---

### Строка 1845

**Проблема:** v50.PROC.ID_SIZE.п.3.27 - Размер идентификатора бизнес-данных (менее 20 символов)

```plp
v_bad_id VARCHAR2(10);
```

**Было  :** `v_bad_id VARCHAR2(10);`
**Станет:** `v_bad_id VARCHAR2(10);`

---

