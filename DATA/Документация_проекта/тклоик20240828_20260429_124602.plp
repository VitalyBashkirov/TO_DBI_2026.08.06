-- Тестовый файл для проверки правил: тклоик20240828
-- Дата генерации: 2026-04-29 12:46:02

--[62] тклоик20240828.стр.1
-- prefix,psh
-- Префикс `PSH_` для объектов Банка

begin --<добавлено при генерации теста>
  Объекты, создаваемые сотрудниками Банка, должны иметь префикс `PSH_` в коротком имени (вместо `FTC_`).
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[63] тклоик20240828.стр.1
-- prefix,bank
-- Префикс банка `[ПСБ]` для локальных объектов

begin --<добавлено при генерации теста>
  Локальные объекты банка должны содержать префикс банка `[ПСБ]` согласно справочнику.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[64] тклоик20240828.стр.1
-- ooxml,reports
-- Новые отчёты в формате OOXML

begin --<добавлено при генерации теста>
  При разработке новых локальных отчётов Word и Excel использовать формат OOXML.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[65] тклоик20240828.стр.1
-- cyrillic,encoding
-- Запрет кириллицы в вычисляемых кодах

begin --<добавлено при генерации теста>
  Вычисляемые коды и параметры не должны содержать кириллицу.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[66] тклоик20240828.стр.1
-- extension,hook
-- Флаг «Объединённый пакет» для операций расширения

begin --<добавлено при генерации теста>
  У новых операций расширения должен быть установлен флаг «Объединённый пакет». Удалить макросы `&BASE$SETVARS` и `&BASE$GETVARS`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[67] тклоик20240828.стр.2
-- view,default
-- Умолчательное представление `VW_CRIT_ИМЯ_КЛАССА`

begin --<добавлено при генерации теста>
  В новых классах должно быть представление «Полный список» с именем `VW_CRIT_ИМЯ_КЛАССА`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[68] тклоик20240828.стр.2
-- view,filter
-- Установка глобальных переменных только в EXECUTE

begin --<добавлено при генерации теста>
  В операциях-фильтрах установку глобальных переменных делать только в секции EXECUTE, основываясь на параметрах операции.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[69] тклоик20240828.стр.2
-- user,userid
-- Замена `USER` на `[RUNTIME].[STDLIB].userid`

begin --<добавлено при генерации теста>
  В представлениях вместо `USER` использовать `::[RUNTIME].[STDLIB].userid`. Функция `USER` возвращает `APP_SRV` в 3-уровневой системе.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[70] тклоик20240828.стр.2
-- naming,underscore
-- Идентификаторы с подчёркиваниями

begin --<добавлено при генерации теста>
  Имена функций, процедур, переменных писать с разделением подчёркиванием: `OPER_DATE`. Допускается `camelCase` без подчёркиваний.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[71] тклоик20240828.стр.2
-- syntax,reference
-- Правильный синтаксис обращения к объектам

begin --<добавлено при генерации теста>
  Указывать полный путь: `:[ТБП].[ОПЕРАЦИЯ].<объект>`. Короткие имена типов в квадратных скобках с двумя двоеточиями: `::[TYPE_NAME]`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[72] тклоик20240828.стр.3
-- this,initialization
-- Запрет `THIS` для инициализации

begin --<добавлено при генерации теста>
  Не использовать `THIS` для инициализации переменных в секции деклараций операций (Конструктор, Простая, Отчёт и др.).
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[73] тклоик20240828.стр.3
-- libraries,reuse
-- Использование существующих библиотек

begin --<добавлено при генерации теста>
  Прежде чем писать свою функцию, проверить библиотеки `RUNTIME`, `StdLib`, `F`, `FD`, `FDP`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[74] тклоик20240828.стр.3
-- objects,performance
-- Запрет запросов к `OBJECTS`, `COL2OBJ`, `REFS`

begin --<добавлено при генерации теста>
  Запросы к этим представлениям негативно сказываются на производительности.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[75] тклоик20240828.стр.3
-- modifier
-- Запрет нескольких модификаторов `%` подряд

begin --<добавлено при генерации теста>
  Не использовать более одного модификатора `%` подряд, если тип после первого неизвестен (приводит к запросам к `OBJECTS`).
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[76] тклоик20240828.стр.4
-- varchar2,size
-- Указывать размерность VARCHAR2/STRING

begin --<добавлено при генерации теста>
  Всегда указывать размерность для `VARCHAR2` и `STRING`. Размерность должна соответствовать хранимым данным.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[77] тклоик20240828.стр.4
-- integer,id
-- Запрет `INTEGER` для ID экземпляров

begin --<добавлено при генерации теста>
  ID экземпляров могут превышать диапазон `PLS_INTEGER`. Использовать `NUMBER` или больший тип.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[78] тклоик20240828.стр.5
-- goto,control
-- Запрет `GOTO`

begin --<добавлено при генерации теста>
  Синтаксис `GOTO <LABEL>` запрещён. Переписывать алгоритм.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[79] тклоик20240828.стр.5
-- savepoint,rollback
-- Запрет прямых `SAVEPOINT`/`ROLLBACK`

begin --<добавлено при генерации теста>
  Использовать макросы для генерации уникальных имён точек сохранения (например, `&sp(sp)`).
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[80] тклоик20240828.стр.6
-- variable,prefix
-- Префикс `v_` для локальных переменных

begin --<добавлено при генерации теста>
  Локальные переменные операции должны начинаться с префикса `v_`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[81] тклоик20240828.стр.6-7
-- constant,prefix
-- Префикс `cn_` для локальных констант

begin --<добавлено при генерации теста>
  Локальные константы должны начинаться с префикса `cn_`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[82] тклоик20240828.стр.7
-- parameter,prefix
-- Префикс `p_` для параметров

begin --<добавлено при генерации теста>
  Параметры процедур и функций должны начинаться с префикса `p_`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[83] тклоик20240828.стр.7
-- cursor,prefix
-- Префикс `cur_` для курсоров

begin --<добавлено при генерации теста>
  Локальные курсоры должны начинаться с префикса `cur_`.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[84] тклоик20240828.стр.7
-- global,variable
-- Запрет глобальных переменных

begin --<добавлено при генерации теста>
  Запрещено использование глобальных переменных, влияющих на логику операции.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[85] тклоик20240828.стр.8
-- case,keywords
-- Служебные слова строчными буквами

begin --<добавлено при генерации теста>
  Служебные слова и операторы языка писать строчными буквами.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================

--[86] тклоик20240828.стр.8
-- plsql,insert
-- Запрет PL/SQL вставок

begin --<добавлено при генерации теста>
  Запрещено использовать PL/SQL вставки. Переписывать на PL+.
  exit; --<добавлено при генерации теста>
end; --<добавлено при генерации теста>

--============================================================
