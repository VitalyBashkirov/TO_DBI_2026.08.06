class PSH_RFM_CTRL;

@name('[ПСБ] Служебная. Очистить справочник')
static method PSH_DELETE_ALL is
PSH_DELETE_ALL is

execute is
begin
	for (select x(x : x_id)
		in ::[PSH_RFM_CTRL]
	)
	loop
		x.x_id.[PSH_DELETE];
	end loop;
end;
end;
end;
