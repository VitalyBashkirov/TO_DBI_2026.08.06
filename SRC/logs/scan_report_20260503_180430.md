# Отчёт анализа PLPlus кода

Дата: 2026-05-03 18:04:30
Всего найдено проблем: 35

## Файл: F:\TO_DBI\PATCH_IN\patch_RV\src\ENTITY\AC_FIN\PSH_311P_CHK.plp

### Строка 14

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure Draw(P_DATE_START	date, P_DATE date)
```

**Было  :** `procedure Draw(P_DATE_START	date, P_DATE date)`
**Станет:** `procedure Draw(P_DATE_START	date, P_DATE date)`

---

### Строка 14

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure Draw(P_DATE_START	date, P_DATE date)
```

**Было  :** `procedure Draw(P_DATE_START	date, P_DATE date)`
**Станет:** `procedure Draw(P_DATE_START	date, P_DATE date)`

---

### Строка 74

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
						 	ac%id = gj.[ACCOUNT](true)
```

**Было  :** `						 	ac%id = gj.[ACCOUNT](true)`
**Станет:** `						 	ac%id = gj.[ACCOUNT](true)`

---

### Строка 74

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Конструкция like ac%id = gj.[ACCOUNT](true)

```plp
						 	ac%id = gj.[ACCOUNT](true)
```

**Было  :** `						 	ac%id = gj.[ACCOUNT](true)`
**Станет:** `						 	ac%id = gj.[ACCOUNT](true)`

---

### Строка 75

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку (без квадратных скобок) с параметром true

```plp
				 		and gj.[IN_FILE_HISTORY] = st.collection_id(true)
```

**Было  :** `				 		and gj.[IN_FILE_HISTORY] = st.collection_id(true)`
**Станет:** `				 		and gj.[IN_FILE_HISTORY] = st.collection_id(true)`

---

## Файл: F:\TO_DBI\PATCH_IN\patch_RV\src\ENTITY\RES_BASE_ACCS\PSH_REP_PROV_XL.plp

### Строка 18

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE 	date
```

**Было  :** `	 ,P_DATE 	date`
**Станет:** `	 ,P_DATE 	date`

---

### Строка 43

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE date`

---

### Строка 67

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE  date
```

**Было  :** `	 ,P_DATE  date`
**Станет:** `	 ,P_DATE  date`

---

### Строка 147

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE		date
```

**Было  :** `	,P_DATE		date`
**Станет:** `	,P_DATE		date`

---

### Строка 150

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
p_date_prev		date;
```

**Было  :** `p_date_prev		date;`
**Станет:** `p_date_prev		date;`

---

### Строка 195

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE 	date
```

**Было  :** `	 ,P_DATE 	date`
**Станет:** `	 ,P_DATE 	date`

---

### Строка 210

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))
```

**Было  :** `		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))`
**Станет:** `		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))`

---

### Строка 220

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))
```

**Было  :** `		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))`
**Станет:** `		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))`

---

### Строка 234

**Проблема:** v50.SQL.UDF.п.1.3 - UDF в WHERE

```plp
		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))
```

**Было  :** `		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))`
**Станет:** `		       where ((z.[DATE_BEGIN] <= P_DATE and not p_prev) or (z.[DATE_BEGIN] < P_DATE and p_prev))`

---

### Строка 249

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE date`

---

### Строка 270

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE date`

---

### Строка 287

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE date`

---

### Строка 308

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE 	date
```

**Было  :** `	 ,P_DATE 	date`
**Станет:** `	 ,P_DATE 	date`

---

### Строка 412

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
     out_debt_date 	 date;
```

**Было  :** `     out_debt_date 	 date;`
**Станет:** `     out_debt_date 	 date;`

---

### Строка 486

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	date_first		date;
```

**Было  :** `	date_first		date;`
**Станет:** `	date_first		date;`

---

### Строка 542

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
					P_DATE	date
```

**Было  :** `					P_DATE	date`
**Станет:** `					P_DATE	date`

---

### Строка 903

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_kred(P_DATE date)
```

**Было  :** `procedure calc_kred(P_DATE date)`
**Станет:** `procedure calc_kred(P_DATE date)`

---

### Строка 948

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and res.[ACC](true) = ac
```

**Было  :** `			and res.[ACC](true) = ac`
**Станет:** `			and res.[ACC](true) = ac`

---

### Строка 989

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_gar(P_DATE date)
```

**Было  :** `procedure calc_gar(P_DATE date)`
**Станет:** `procedure calc_gar(P_DATE date)`

---

### Строка 1034

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and (res.[ACC](true) = ac or res.[PROD_RES](true) = port)
```

**Было  :** `			and (res.[ACC](true) = ac or res.[PROD_RES](true) = port)`
**Станет:** `			and (res.[ACC](true) = ac or res.[PROD_RES](true) = port)`

---

### Строка 1034

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and (res.[ACC](true) = ac or res.[PROD_RES](true) = port)
```

**Было  :** `			and (res.[ACC](true) = ac or res.[PROD_RES](true) = port)`
**Станет:** `			and (res.[ACC](true) = ac or res.[PROD_RES](true) = port)`

---

### Строка 1036

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and port.[ACCRS](true) = port_acc%collection
```

**Было  :** `			and port.[ACCRS](true) = port_acc%collection`
**Станет:** `			and port.[ACCRS](true) = port_acc%collection`

---

### Строка 1037

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and port_acc.[RES_ACC](true) = ac
```

**Было  :** `			and port_acc.[RES_ACC](true) = ac`
**Станет:** `			and port_acc.[RES_ACC](true) = ac`

---

### Строка 1038

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and port_acc.[DATE_BEGIN](true) <= P_DATE
```

**Было  :** `			and port_acc.[DATE_BEGIN](true) <= P_DATE`
**Станет:** `			and port_acc.[DATE_BEGIN](true) <= P_DATE`

---

### Строка 1039

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and (port_acc.[DATE_END](true) is null or port_acc.[DATE_END](true) > P_DATE)
```

**Было  :** `			and (port_acc.[DATE_END](true) is null or port_acc.[DATE_END](true) > P_DATE)`
**Станет:** `			and (port_acc.[DATE_END](true) is null or port_acc.[DATE_END](true) > P_DATE)`

---

### Строка 1039

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and (port_acc.[DATE_END](true) is null or port_acc.[DATE_END](true) > P_DATE)
```

**Было  :** `			and (port_acc.[DATE_END](true) is null or port_acc.[DATE_END](true) > P_DATE)`
**Станет:** `			and (port_acc.[DATE_END](true) is null or port_acc.[DATE_END](true) > P_DATE)`

---

### Строка 1075

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_mbk(P_DATE date)
```

**Было  :** `procedure calc_mbk(P_DATE date)`
**Станет:** `procedure calc_mbk(P_DATE date)`

---

### Строка 1119

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Внешнее соединение через реквизит-ссылку с параметром true

```plp
			and res.[ACC](true) = ac
```

**Было  :** `			and res.[ACC](true) = ac`
**Станет:** `			and res.[ACC](true) = ac`

---

### Строка 1159

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_POT(P_DATE date)
```

**Было  :** `procedure calc_POT(P_DATE date)`
**Станет:** `procedure calc_POT(P_DATE date)`

---

### Строка 1203

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_other(P_DATE date)
```

**Было  :** `procedure calc_other(P_DATE date)`
**Станет:** `procedure calc_other(P_DATE date)`

---

