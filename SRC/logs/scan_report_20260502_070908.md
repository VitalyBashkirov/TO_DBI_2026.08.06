# Отчёт анализа PLPlus кода

Дата: 2026-05-02 07:09:08
Всего найдено проблем: 35

## Файл: F:\TO_DBI\PATCH_IN\patch_RV\src\ENTITY\AC_FIN\PSH_311P_CHK.plp

### Строка 3

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
@import_plsql(true)
```

**Было  :** `@import_plsql(true)`
**Станет:** `DATE_TIME(true)`

---

### Строка 4

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
@this(plsql_lock:=false)
```

**Было  :** `@this(plsql_lock:=false)`
**Станет:** `DATE_TIME(plsql_lock:=false)`

---

### Строка 5

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
@name('[ПСБ] 311-П.  Контроль сообщений об открытых и закрытых счетах')
```

**Было  :** `@name('[ПСБ] 311-П.  Контроль сообщений об открытых и закрытых счетах')`
**Станет:** `DATE_TIME('[ПСБ] 311-П.  Контроль сообщений об открытых и закрытых счетах')`

---

### Строка 14

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure Draw(P_DATE_START	date, P_DATE date)
```

**Было  :** `procedure Draw(P_DATE_START	date, P_DATE date)`
**Станет:** `procedure Draw(P_DATE_START	DATE_TIME, P_DATE DATE_TIME)`

---

### Строка 14

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure Draw(P_DATE_START	date, P_DATE date)
```

**Было  :** `procedure Draw(P_DATE_START	date, P_DATE date)`
**Станет:** `procedure Draw(P_DATE_START	DATE_TIME, P_DATE DATE_TIME)`

---

### Строка 74

**Проблема:** v50.SQL.OUTERJOIN.п.1.1 - Конструкция like ac%id = gj.[ACCOUNT](true)

```plp
						 	ac%id = gj.[ACCOUNT](true)
```

**Было  :** `						 	ac%id = gj.[ACCOUNT](true)`
**Станет:** `						 	DATE_TIME`

---

### Строка 199

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Дата отчета')          P_DATE       in [DATE],
```

**Было  :** `	@name('Дата отчета')          P_DATE       in [DATE],`
**Станет:** `	DATE_TIME('Дата отчета')          P_DATE       in [DATE],`

---

### Строка 200

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Выбрать счета с даты') P_DATE_START in [DATE]
```

**Было  :** `	@name('Выбрать счета с даты') P_DATE_START in [DATE]`
**Станет:** `	DATE_TIME('Выбрать счета с даты') P_DATE_START in [DATE]`

---

## Файл: F:\TO_DBI\PATCH_IN\patch_RV\src\ENTITY\RES_BASE_ACCS\PSH_REP_PROV_XL.plp

### Строка 3

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
@import_plsql(true)
```

**Было  :** `@import_plsql(true)`
**Станет:** `DATE_TIME(true)`

---

### Строка 4

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
@name('[ПСБ] Контроль создания регуляторных и оценочных резервов (Excel)')
```

**Было  :** `@name('[ПСБ] Контроль создания регуляторных и оценочных резервов (Excel)')`
**Станет:** `DATE_TIME('[ПСБ] Контроль создания регуляторных и оценочных резервов (Excel)')`

---

### Строка 18

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE 	date
```

**Было  :** `	 ,P_DATE 	date`
**Станет:** `	 ,P_DATE 	DATE_TIME`

---

### Строка 43

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE DATE_TIME`

---

### Строка 67

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE  date
```

**Было  :** `	 ,P_DATE  date`
**Станет:** `	 ,P_DATE  DATE_TIME`

---

### Строка 147

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE		date
```

**Было  :** `	,P_DATE		date`
**Станет:** `	,P_DATE		DATE_TIME`

---

### Строка 150

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
p_date_prev		date;
```

**Было  :** `p_date_prev		date;`
**Станет:** `p_date_prev		DATE_TIME;`

---

### Строка 195

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE 	date
```

**Было  :** `	 ,P_DATE 	date`
**Станет:** `	 ,P_DATE 	DATE_TIME`

---

### Строка 249

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE DATE_TIME`

---

### Строка 270

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE DATE_TIME`

---

### Строка 287

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	,P_DATE date
```

**Было  :** `	,P_DATE date`
**Станет:** `	,P_DATE DATE_TIME`

---

### Строка 308

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	 ,P_DATE 	date
```

**Было  :** `	 ,P_DATE 	date`
**Станет:** `	 ,P_DATE 	DATE_TIME`

---

### Строка 412

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
     out_debt_date 	 date;
```

**Было  :** `     out_debt_date 	 date;`
**Станет:** `     out_debt_date 	 DATE_TIME;`

---

### Строка 486

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
	date_first		date;
```

**Было  :** `	date_first		date;`
**Станет:** `	date_first		DATE_TIME;`

---

### Строка 542

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
					P_DATE	date
```

**Было  :** `					P_DATE	date`
**Станет:** `					P_DATE	DATE_TIME`

---

### Строка 903

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_kred(P_DATE date)
```

**Было  :** `procedure calc_kred(P_DATE date)`
**Станет:** `procedure calc_kred(P_DATE DATE_TIME)`

---

### Строка 989

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_gar(P_DATE date)
```

**Было  :** `procedure calc_gar(P_DATE date)`
**Станет:** `procedure calc_gar(P_DATE DATE_TIME)`

---

### Строка 1075

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_mbk(P_DATE date)
```

**Было  :** `procedure calc_mbk(P_DATE date)`
**Станет:** `procedure calc_mbk(P_DATE DATE_TIME)`

---

### Строка 1159

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_POT(P_DATE date)
```

**Было  :** `procedure calc_POT(P_DATE date)`
**Станет:** `procedure calc_POT(P_DATE DATE_TIME)`

---

### Строка 1203

**Проблема:** v50.STOR.DATE.п.2.2 - Тип DATE

```plp
procedure calc_other(P_DATE date)
```

**Было  :** `procedure calc_other(P_DATE date)`
**Станет:** `procedure calc_other(P_DATE DATE_TIME)`

---

### Строка 1241

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Дата отчёта')                   P_DATE  in [DATE],
```

**Было  :** `	@name('Дата отчёта')                   P_DATE  in [DATE],`
**Станет:** `	DATE_TIME('Дата отчёта')                   P_DATE  in [DATE],`

---

### Строка 1242

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Отчёт по кредитам')             P_KRED  in [BOOLEAN],
```

**Было  :** `	@name('Отчёт по кредитам')             P_KRED  in [BOOLEAN],`
**Станет:** `	DATE_TIME('Отчёт по кредитам')             P_KRED  in [BOOLEAN],`

---

### Строка 1243

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Отчёт по страховым депозитам')  P_DEPN  in [BOOLEAN],
```

**Было  :** `	@name('Отчёт по страховым депозитам')  P_DEPN  in [BOOLEAN],`
**Станет:** `	DATE_TIME('Отчёт по страховым депозитам')  P_DEPN  in [BOOLEAN],`

---

### Строка 1244

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Отчёт по МБК')                  P_MBK   in [BOOLEAN],
```

**Было  :** `	@name('Отчёт по МБК')                  P_MBK   in [BOOLEAN],`
**Станет:** `	DATE_TIME('Отчёт по МБК')                  P_MBK   in [BOOLEAN],`

---

### Строка 1245

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Отчёт по гарантиям')            P_GAR   in [BOOLEAN],
```

**Было  :** `	@name('Отчёт по гарантиям')            P_GAR   in [BOOLEAN],`
**Станет:** `	DATE_TIME('Отчёт по гарантиям')            P_GAR   in [BOOLEAN],`

---

### Строка 1246

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Отчёт по ПОТ (кроме гарантий)') P_POT   in [BOOLEAN],
```

**Было  :** `	@name('Отчёт по ПОТ (кроме гарантий)') P_POT   in [BOOLEAN],`
**Станет:** `	DATE_TIME('Отчёт по ПОТ (кроме гарантий)') P_POT   in [BOOLEAN],`

---

### Строка 1247

**Проблема:** v50.INT.DBLINK.п.4.2 - Database Link

```plp
	@name('Отчёт по прочим счетам')        P_OTHER in [BOOLEAN]
```

**Было  :** `	@name('Отчёт по прочим счетам')        P_OTHER in [BOOLEAN]`
**Станет:** `	DATE_TIME('Отчёт по прочим счетам')        P_OTHER in [BOOLEAN]`

---

