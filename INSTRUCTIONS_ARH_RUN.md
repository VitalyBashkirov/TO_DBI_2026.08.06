# Инструкция по запуску АРМ "Адаптация под DBI"

## 1. Запуск приложения

```bash
cd F:\TO_DBI
python SRC/gui_app.py
```

## 2. Настройка путей

### Поле "Исходный каталог":
```
F:\TO_DBI\PATCH_IN\patch_RV
```

### Поле "Каталог результатов":
Автоматически заполнится как:
```
F:\TO_DBI\PATCH_OUT\patch_RV
```

## 3. Выбор правил в рубрикаторе

В дереве рубрикатора **обязательно** отметьте чекбоксы:
- `[✓] v50` — Рекомендации по адаптации кода на PLPlus для DBI

## 4. Запуск сканирования

Нажмите кнопку **"🔍 Сканировать"** (или F5)

### Ожидаемый результат:
```
[ИСПОЛЬЗУЕТСЯ] Расширенный рубрикатор 4.RUBRICATOR_PROMPTS.json
  Версия: 2.2.0

ИСПОЛЬЗУЕМЫЕ ПРАВИЛА (1):
  [+] v50

ПРАВИЛА ЗАГРУЖЕНЫ ИЗ JSON:
  v50.SQL.OUTERJOIN.п.1.1: 7 паттернов
  v50.SQL.ROWNUM.п.1.2: 2 паттерна
  ...

[1/3] Сканирование завершено...
  Найдено *.plp файлов: N
  Проблемных конструкций: N
```

## 5. Проверка нахождения проблемы `gj.[ACCOUNT](true)`

В журнале выполнения должно быть:
```
  Строка 74 >>> ac%id = gj.[ACCOUNT](true)
    [ТЕГИ] outer join, oracle, ansi [КОД] v50.SQL.OUTERJOIN.п.1.1: Конструкция like ac%id = gj.[ACCOUNT](true)
    [ПРОМПТ] Проанализируй код: ac%id = gj.[ACCOUNT](true) | Правило: v50.SQL.OUTERJOIN.п.1.1 | ...
```

## 6. Возможные проблемы и решения

### Проблема: "Не найдено проблем"

**Причина 1:** Не выбрано правило в рубрикаторе
- **Решение:** Отметьте чекбокс `v50` в дереве рубрикатора

**Причина 2:** Неверный исходный каталог
- **Решение:** Укажите `F:\TO_DBI\PATCH_IN\patch_RV`

**Причина 3:** Файлы не `.plp`
- **Решение:** Проверьте, что в каталоге есть `.plp` файлы

### Проблема: "Правило v50.SQL.OUTERJOIN.п.1.1 не найдено"

**Причина:** Не загружен JSON-рубрикатор
- **Решение:** Проверьте наличие файла `DATA/Рубрикатор/4.RUBRICATOR_PROMPTS.json`

## 7. Проверка работы рубрикатора

Для проверки запустите тестовый скрипт:
```bash
python test_gui_rubricator.py
```

Ожидаемый вывод:
```
[OK] Рубрикатор загружен (версия 2.2.0)
[OK] Правило 'v50.SQL.OUTERJOIN.п.1.1' найдено:
  Паттерны для поиска (7):
    - (\w+)\.\[(\w+)\]\(true\)
    - (\w+)%?(\w+)\s*=\s*(\w+)\.\[(\w+)\]\(true\)
  Найдено проблем: 3
    Строка 74: ac%id = gj.[ACCOUNT](true)
    Строка 75: gj.[IN_FILE_HISTORY] = st.collection_id(true)
```

## 8. Отчёт о сканировании

После сканирования отчёт сохраняется в:
```
SRC/logs/scan_report_<имя_каталога>_<дата>.md
```

## 9. Исправление кода

После успешного сканирования нажмите **"✏️ Исправить код"** (F6)

Исправленные файлы будут в:
```
F:\TO_DBI\PATCH_OUT\patch_RV\patch_RV_v<дата>
```
